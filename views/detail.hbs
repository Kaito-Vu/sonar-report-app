<!DOCTYPE html>
<html>
<head>
  <title>Report Detail</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container-fluid py-4">
<a href="/" class="btn btn-outline-secondary mb-3">&larr; Quay lại</a>
<h2>Chi tiết báo cáo: {{report.filename}}</h2>

<table class="table table-striped table-hover mt-3 table-sm">
  <thead class="table-dark">
  <tr>
    <th>Severity</th>
    <th>Rule Name</th>
    <th>File Name</th>
    <th>Line</th>
    <th>Message</th>
  </tr>
  </thead>
  <tbody>
  {{#each report.issues}}
    <tr>
      <td><span class="badge bg-secondary">{{this.severity}}</span></td>
      <td>{{this.ruleName}}</td>
      <td class="text-break" style="max-width: 300px;">{{this.fileName}}</td>
      <td>{{this.fileLine}}</td>
      <td>{{this.message}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>
</body>
</html>