<!DOCTYPE html>
<html>
<head>
  <title>Cấu hình SonarQube</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
<h2>Cấu hình kết nối SonarQube API</h2>
<a href="/" class="btn btn-outline-secondary mb-3">Quay lại</a>

<div class="card p-4 shadow-sm">
  <form action="/settings" method="post">
    <div class="mb-3">
      <label class="form-label">SonarQube URL</label>
      <input type="text" name="url" class="form-control" placeholder="http://localhost:9000" value="{{config.url}}" required>
      <div class="form-text">Địa chỉ server SonarQube của bạn (không bao gồm dấu / ở cuối)</div>
    </div>
    <div class="mb-3">
      <label class="form-label">User Token</label>
      <input type="password" name="token" class="form-control" value="{{config.token}}" required>
      <div class="form-text">Generate tại: User Profile > Security > Tokens</div>
    </div>
    <button type="submit" class="btn btn-primary">Lưu cấu hình</button>
  </form>
</div>
</body>
</html>